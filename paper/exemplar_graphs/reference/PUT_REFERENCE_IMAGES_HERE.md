Drop the two reference figures here:

    fig12_trajectory_keyframes.png    trajectory + keyframes, two scenes (box, door)
    fig13_skill_strip.png             skill execution across objects and door types

They are gitignored -- they are someone else's figures and this repo is public.
They live here so the layout is in front of us while we build ours, not to be shipped.
